#!/bin/sh

brew update
brew upgrade

TAPS=(
    homebrew/dupes
    homebrew/versions
    homebrew/services
)
brew tap ${TAPS[@]}

FORMULAS=(
    rbenv
    ruby-build
    git
    imagemagick
    libxml2
    libxslt
    vim
)
brew install ${FORMULAS[@]}

brew cleanup

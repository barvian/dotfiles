#!/bin/sh

brew update
brew upgrade

TAPS=(
    homebrew/dupes
    homebrew/versions
    homebrew/services
)
brew tap ${TAPS[@]}

FORMULAS=(
    git
    imagemagick
    libxml2
    libxslt
)
brew install ${FORMULAS[@]}

brew cleanup
